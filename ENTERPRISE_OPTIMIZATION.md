# ReqFlow 企业级优化记录

## 当前判断

ReqFlow 已经具备 AI-Native 需求分析工具的核心雏形：会话、需求宪法、受控提案、版本快照、质量闸门、规格包导出和 OpenAI 兼容 LLM 编排。项目当前更接近“单人本地工作台”，要进入企业级适用，需要优先提升可交付性、一致性、安全边界、可观测性和自动化验证。

## 已落地优化

### 质量闸门增强

`server/src/store.ts` 中的 `runQualityCheck` 已从基础完整性检查升级为交付一致性检查，新增规则包括：

- 冻结前不能保留需求宪法占位内容。
- 高影响待确认问题在审阅/冻结阶段会阻断推进。
- 功能 ID 必须唯一。
- 功能关联场景会被校验，发现不存在的场景会预警。
- 冻结前必须具备技术方案。
- 验收标准不能引用不存在的功能。
- 冻结时 P0/P1 功能必须有验收覆盖。
- 验收用例 ID 必须唯一。
- 任务依赖必须引用存在的任务。
- 任务必须能追溯到存在的验收用例。
- 技术方案 API 路径必须以 `/` 开头，重复 API 路径会预警。

### 自动化测试

新增 `server/tests/store.test.ts`，覆盖：

- 接受提案后正式文档、阶段和版本快照的受控变更链路。
- 冻结交付前的验收覆盖、任务依赖和任务到验收用例追溯检查。

新增 `server/tests/api.test.ts`，覆盖：

- 会话创建接口的输入校验。
- 提案通过 API 接受后的状态更新。
- 质量检查接口返回交付一致性检查结果。
- Markdown 规格包导出包含技术方案、验收标准、任务拆解和决策记录。

新增 `server/tests/llm.integration.test.ts` 和 `npm run test:llm`，用于真实调用本机或远程 OpenAI 兼容 API，校验 LLM 能返回可解析的结构化需求提案。默认 `npm test` 不调用真实模型，避免 CI 和本地快速测试产生模型成本。

已用 DeepSeek OpenAI 兼容 API 跑通过真实 LLM 集成测试，模型为 `deepseek-v4-flash`。

### LLM 结构化输出恢复

`server/src/llm.ts` 已增加结构化输出解析和自动修复重试：当模型返回无效 JSON、字段结构不合格或缺少本轮必需文档时，系统会把校验错误和字段模板发回模型，要求重新输出一次完整 JSON。修复失败后错误会带上 provider 信息，便于定位模型、网关或提示词问题。

### 自循环可用性

前端对话区新增流程教练，根据当前会话状态直接给出下一步动作：生成需求草案、接受提案、回答待确认问题、生成验收标准和任务拆解、生成技术方案、生成原型、运行质量检查和冻结规格包。接受需求提案时，会话级待确认问题会从正式需求文档同步，避免旧问题残留导致流程误导。

### 需求追溯矩阵

文档区新增追溯矩阵 Tab，把每个功能与验收用例、开发任务、技术模块/API 线索放在同一视图中，标记缺验收、缺任务和缺技术线索的断链项。流程教练会在进入原型校准前提示先查看追溯矩阵，帮助用户判断需求拆分是否完整。

### AI 决策辅助

流程教练新增 AI 决策辅助入口，不再假设用户具备产品、技术或架构能力。用户可以让 AI 推荐默认第一版方向、给待确认问题提供选项、检查关键假设和验证办法、解释是否应该接受提案、生成更保守版本、解释验收标准、推荐稳妥技术方案，并把质量问题按优先级拆成必须修、建议修和可暂缓。

后端新增只读 `/advise` 模式，用于解释、建议、对比和排序，不创建变更提案、不覆盖待确认提案、不修改正式文档。真实 DeepSeek 集成测试已覆盖“生成提案后请求 advice，pending proposal 保持不变”的行为。

### 依赖安全

后端 `uuid` 已升级到 `14.0.0`，并移除不再需要的 `@types/uuid`，生产依赖审计当前为 0 漏洞。

### 规格包导出增强

`exportMarkdown` 已从摘要导出升级为完整规格包导出，包含需求宪法、需求文档、技术方案、验收标准、任务拆解、风险记录、决策记录、待确认问题、版本快照和质量报告。

### 审计日志

会话模型新增 `auditEvents`，记录企业交付中需要追踪的关键动作：创建会话、阶段切换、质量检查、提案接受/拒绝、回滚、原型生成和规格包导出。后端新增 `GET /api/session/:id/audit`，并支持通过 `x-reqflow-actor` 请求头传入操作者标识，为后续接入企业身份系统预留接口。

前端已增加操作者标识设置，并在创建会话、对话生成、提案接受/拒绝、质量检查、回滚、原型生成和规格包导出时自动透传 `x-reqflow-actor`，使审计日志不再只能记录默认本地用户。

### 工作区隔离

会话模型新增 `workspaceId`，创建会话时从 `x-reqflow-workspace` 写入工作区，后续读取、对话、提案确认、质量检查、评审、回滚、原型生成和导出都必须使用同一个工作区请求头。工作区不匹配时后端返回 403，避免不同团队或项目误读、误改需求资产。

前端身份控件已同时支持工作区和操作者设置，所有 API 请求会自动透传 `x-reqflow-workspace` 与 `x-reqflow-actor`。这不是完整登录/RBAC，但已经把企业多项目隔离边界纳入核心数据模型和服务端校验。

后端新增 `GET /api/session`，按请求工作区返回会话摘要列表；首页会展示当前工作区的最近会话，用户可以直接继续已有需求项目，不再只依赖浏览器里的最后一次会话。

### 轻量角色权限

后端新增 `x-reqflow-role` 角色边界：`viewer` 只读，`reviewer` 可运行质量检查和提交评审，`editor` 可发起 AI 变更、接受/拒绝提案和生成原型，`admin` 可阶段切换和回滚。前端身份控件已支持角色选择，并在请求中自动透传。未传角色时按本地 `admin` 处理，便于本地开发；企业接入时可由网关或身份系统注入真实角色。

服务端新增 `REQFLOW_AUTH_MODE=trusted-header`，用于企业可信网关模式。开启后，除 `/api/health` 和 `/api/ready` 外的 API 必须携带 `x-reqflow-actor`、`x-reqflow-workspace` 和合法 `x-reqflow-role`，避免生产环境继续依赖浏览器可随意填写的身份信息。

### 评审签核

会话模型新增 `reviews`，记录当前版本的评审人、角色、通过/打回状态和意见。后端新增 `POST /api/session/:id/reviews`，评审动作会写入审计日志；冻结规格包时，质量闸门要求当前版本至少一条通过记录，并且当前版本不能存在打回记录。

前端新增“评审签核”文档页，支持业务负责人或评审人提交通过/打回意见。流程教练会在冻结前提示先完成当前版本评审，避免未经确认的规格包进入交付。

### LLM 评测基准

`server/evals/llm-eval-cases.json` 提供固定评测集，覆盖企业需求分析工作台、非技术运营用户和有审计要求的审批流程三类场景。`npm run eval:llm` 会调用真实模型，检查结构化需求提案、P0 功能、范围边界、领域关键词、只读 advice 不改状态，以及接受提案后的质量检查结果。

评测默认只跑低成本核心链路；设置 `REQFLOW_EVAL_FULL=1` 后，会追加验收标准、任务拆解和技术方案生成评测。评测输出为 JSON，便于接入 CI 或模型升级前对比。

### 网站流程烟测

前端新增 Playwright 烟测 `client/e2e/smoke.spec.ts` 和 `npm run test:e2e`。该测试会真实打开网站，走通首页输入想法、等待 LLM 生成第一版需求提案、应用草案、发送 `/generate-acceptance`、等待验收标准和任务拆解提案、再次应用草案的关键路径，用来验证网站整体流程不是只在 API 层可用。

工作台顶部新增“导出规格包”按钮，直接调用 Markdown 导出 API 并触发浏览器下载。烟测会覆盖该按钮，确认网站端可以拿到最终交付物，不需要手动调用接口。

### 部署配置

根目录新增 `Dockerfile`、`docker-compose.yml`、`.dockerignore` 和 `DEPLOYMENT.md`。生产模式下 Express 可通过 `CLIENT_DIST_DIR` 托管构建后的 React 静态资源，形成单端口服务；`REQFLOW_DATA_DIR` 可挂载到持久化数据卷，`/api/ready` 可作为容器 readiness 健康检查。部署文档明确了 DeepSeek/OpenAI 兼容配置、数据目录、审计/工作区/RBAC 头部由可信网关注入的建议。

后端新增 `/api/ready` readiness endpoint，校验 SQLite 查询、数据目录写权限和 LLM 配置；Docker healthcheck 已改为使用 readiness，而 `/api/health` 保持轻量存活检查。

### CI 流水线

新增 `.github/workflows/ci.yml`，在 push/PR 上运行后端测试、前后端构建、依赖审计和 Docker build，不依赖真实模型。新增 `.github/workflows/llm-smoke.yml` 作为手动 workflow，读取 `OPENAI_API_KEY` secret 后用真实 OpenAI 兼容模型运行网站烟测，适合模型升级或上线前验证。

### 请求可观测性

后端新增请求上下文和统一错误响应。每个请求会透传或生成 `x-request-id`，请求日志输出结构化 JSON，JSON 解析错误、未匹配 API 和兜底异常都会返回包含 `requestId` 的 JSON 响应。可通过 `REQFLOW_REQUEST_LOGS=0` 关闭请求日志。

## 推荐路线图

### P0：企业交付可信度

- 为 LLM 输出增加重试、修复提示和结构化错误分类，避免用户只看到通用失败。
- 把需求、验收、任务、技术方案之间的追溯关系在 UI 中显式展示。
- 增加规格包导出完整度：需求全文、技术方案、验收、任务、风险、决策、版本摘要全部导出。
- 增加后端 API 级测试，覆盖会话创建、提案接受/拒绝、回滚、质量检查和导出。

### P1：企业运行能力

- 增加工作区/项目维度，为后续多人协作和权限控制留出数据模型。
- 在前端增加审计日志面板，展示谁在何时接受了哪个提案、修改了哪些资产。
- 增加配置化 CORS、请求大小、数据目录和模型参数。
- 增加结构化业务指标，例如 LLM 调用耗时、修复重试次数、质量闸门失败原因统计。

### P2：AI-Native 深化

- 支持“需求冲突检测”专门流程，把冲突从普通修改中分离出来。
- 增加多 Agent 分工：需求分析、技术架构、验收设计、风险审查。
- 增加企业模板：SaaS、内部系统、数据平台、移动应用、AI Agent。
- 增加规格包评分解释，让质量得分可解释、可追踪、可改进。
